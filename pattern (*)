#include <iostream>
using namespace std;
int main()
{
	int n,i;
	cout<<"enter the number:";
	cin>>n;
	for(i=2;i<=n/2;++i)
	{
		n=n/2;
	}
		if(n%i==0)
		cout<<"it is prime number";
		else
		cout<<"it is not prime number";
		
	
}
